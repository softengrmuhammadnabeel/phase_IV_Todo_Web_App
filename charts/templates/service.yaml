apiVersion: v1
kind: Service
metadata:
  name: {{ include "todoBot.fullname" . }}
  labels:
    {{- include "todoBot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "todoBot.selectorLabels" . | nindent 4 }}
