apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "todoBot.fullname" . }}-test-connection"
  labels:
    {{- include "todoBot.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "todoBot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
